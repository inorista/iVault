import SwiftUI

enum PassVaultColor {
    static let primary = Color(.pvPrimary)
    static let background = Color(.pvBackground)
    static let surface = Color(.pvSurface)
    static let textPrimary = Color(.pvTextPrimary)
    static let textSecondary = Color(.pvTextSecondary)
    static let border = Color(.pvBorder)
    static let danger = Color(.pvDanger)
    static let success = Color(.pvSuccess)
}
